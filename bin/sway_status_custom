#!/bin/bash
# shell scipt to prepend i3status with more stuff
swaybar --config ~/.swaystatus.conf | while :
do
 read line
 LG=$(setxkbmap -query | awk '/layout/{print $2}')
 echo $line || exit 1
#  echo "$LG | $line" || exit 1
done
